function galleryHTML(gallery) {
    let count = 0;
    let html = "";
    for (let i = 0; i <= gallery.length; i++){
        if(count===0){
            html += '<div class="row">'
        }
        html +=
    }
}